import { React } from 'react';
import Footer from './Footer';

function NoPage() {
    return (
        <div>
            <h1 style={{backgroundColor: "black", color: "white"}}>INI HALAMAN ERROR</h1>
            <Footer />
        </div>
    );
}

export default NoPage;